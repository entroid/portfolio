import type { ProjectMeta } from "../../schema";

// Placeholder seed content for Phase 5 — exercises the "other" template.
// Swap for a real case study before shipping (see docs/IMPLEMENTATION_PLAN.md, Phase 13).
export const meta: ProjectMeta = {
  slug: "lumen-crm",
  depth: "other",
  order: 1,
  coverImage: "/images/work/lumen-crm/cover.png",
  coverImageAlt:
    "Lumen CRM's redesigned pipeline board with an inline deal detail panel open alongside it",
  gallery: [],
};
